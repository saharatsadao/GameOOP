[project]
name = "Seer"
version = "0.1.0"
description = "A platformer game built with Pygame"
dependencies = [
    "pygame>=2.5.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
